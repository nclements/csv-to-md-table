Needed a quick-n-dirty way to convert a CSV to a Markdown table. Also wanted to have the option to prepend the result with some extra Markdown (read from a file). And wanted to do it in Rust, just because.

This is the result.
